# ── project data ───────────────────────────────────────────────
download/

# ── generated by the tree-detection pipeline (scoped to the project subtree) ──
tree_detection/gables_campus/yolo_dataset/
tree_detection/gables_campus/runs/
tree_detection/gables_campus/output/

# ── large binaries (never commit surveys or model weights) ──────
*.tif
*.tiff
*.pt
*.pth

# ── python ─────────────────────────────────────────────────────
__pycache__/

# ── jupyter ────────────────────────────────────────────────────
.ipynb_checkpoints/

# ── macs ───────────────────────────────────────────────────────
.DS_Store